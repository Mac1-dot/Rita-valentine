<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Rita, Will You Be My Valentine?</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      text-align: center;
      color: #fff;
      overflow: hidden;
    }

    .card {
      background: rgba(255, 255, 255, 0.18);
      padding: 30px;
      border-radius: 20px;
      max-width: 360px;
      box-shadow: 0 10px 30px rgba(0,0,0,0.2);
      backdrop-filter: blur(10px);
      position: relative;
    }

    h1 {
      font-size: 2em;
      margin-bottom: 10px;
    }

    p {
      font-size: 1.1em;
      margin-bottom: 20px;
    }

    h2 {
      margin-bottom: 20px;
    }

    button {
      font-size: 1em;
      padding: 12px 20px;
      border: none;
      border-radius: 25px;
      margin: 10px;
      cursor: pointer;
      transition: transform 0.2s;
      position: relative;
    }

    .yes {
      background-color: #ff4d6d;
      color: white;
    }

    .no {
      background-color: #ffffff;
      color: #ff4d6d;
      position: absolute;
    }

    button:hover {
      transform: scale(1.1);
    }

    #response {
      margin-top: 20px;
      font-size: 1.3em;
      font-weight: bold;
    }

    .signature {
      margin-top: 20px;
      font-size: 1.1em;
      font-style: italic;
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Hey Rita 💖</h1>

    <p>
      From your smile 😘 to the way you make my heart feel at home 💗,
      every moment with you means everything to me.
      You’re my favorite person, always ❤️
    </p>

    <h2>Rita, will you be my Valentine? 💘💋</h2>

    <button class="yes" onclick="sayYes()">Yes 💕😍</button>
    <button class="no" id="noBtn">No 🙈</button>

    <div id="response"></div>

    <div class="signature">
      Love, Mac 💖😘
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");

    function moveButton() {
      const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
      const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }

    noBtn.addEventListener("mouseover", moveButton);
    noBtn.addEventListener("touchstart", moveButton);

    function sayYes() {
      document.getElementById("response").innerHTML =
        "Yay!! 💖😍 I knew it! Thank you for being my Valentine, Rita 😘💘";
    }
  </script>

</body>
</html>